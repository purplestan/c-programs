#include<stdio.h>
int main()
{
    int i=0;
    char s[100];
    gets(s);
    while(s[i]!='\0')
    {
        if(s[i]=='a')
        {
            s[i]='A';
        }
        i++;
    
    }
    printf("%s",s);
    return 0;
}
