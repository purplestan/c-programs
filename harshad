#include<stdio.h>
void main()
{
  int n,sum=0,r;
  scanf("%d",&n);
  r=n;
  while(n>0)
  {
	sum=sum+n%10;
	n=n/10;
  }
    if(r%sum==0)
	printf("Harshad Number ");
    else
	printf("Not Harshad Number");
}
